# WireMock

## Run stand-alone API

Runs the API on port 8081 as stand-alone application

```shell
java -jar wiremock-jre8-standalone-2.35.0.jar --port 8081
```

## Pending

Using wiremock to mock APIs in java, till now wiremock is standalone tool
but is very powerful